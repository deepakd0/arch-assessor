# ArchAssessor Phase 1 runtime dependencies (pinned exact versions, zero CVE tolerance)
# Audit after any change: pip-audit -r requirements.txt --no-deps --disable-pip

python-hcl2==8.1.2
PyYAML==6.0.3
